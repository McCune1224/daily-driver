version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/db/query.sql"
    schema: "internal/db/schema.sql"
    gen:
      go:
        package: "db"
        sql_package: "pgx/v5"
        out: "internal/db"

# # Project setup for Go + pgx
# packages:
#   - name: "db"
#     path: "internal/db"
#     queries: "internal/db/query/"
#     schema: "internal/db/migrations/"
#     engine: "postgresql"
#     sql_package: "pgx/v5"
#
# # Recommended for development
# sql:
#   dialect: "postgresql"
#   migration_dir: "internal/db/migrations/"
#
